extends ../components/base/layout

block variables
  - const title = "отзывы - Дивайн Лайт"; //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = "";
  - const hasHeader = true;
  - const headerMods = "";
  - const hasFooter = true;
  - const footerMods = "";
  - const currentUrl = ""; //- для остальных страниц пишем пустую строку или индекс активного headerLinks


  //- - const breadcrumbs = [{href: '#', text: 'Отзывы клиентов на 5 звезд'}];

block main
  main.page
    //- +breadcrumbs(breadcrumbs).is-active.breadcrumbs--big
    +great-reviews()

    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks

block modals
  +modal("modal--contacts")(data-modal="contacts")
    +modal-contacts(headerContactsSections, addressList)
  +modal("modal--city")(data-modal="city")
    +modal-city(cityList, searchList)
  +modal("modal--alas")(data-modal="alas")
    +modal-alas()
  +modal("modal--authorization")(data-modal="authorization")
    +modal-authorization()

